const getCurrentUser = (cookieHeaders: string) : object => {
  
  const user = {}

  if (!cookieHeaders.length) return user

  // logic here!!

  return user

}

export {
  getCurrentUser
}